@AGENTS.md

<!--
Claude Code reads this file. The line above imports the shared AGENTS.md so there is one source of truth.
Add Claude-specific notes below — they layer on top of AGENTS.md. Keep genuinely project-wide facts in
AGENTS.md itself so every other tool gets them too.
-->

## Claude Code notes
<!-- e.g.
- Slash commands live in .claude/commands/ — /test runs the suite, /migrate handles schema changes.
- Delegate DB schema work to the `db-migrate` subagent.
- Auto-format runs via a PostToolUse hook; don't hand-format edited files.
-->
