# Shared project settings only. Keep model, provider, approval, and personal UI defaults in
# ~/.codex/config.toml or a named ~/.codex/<profile>.config.toml file.

[agents]
max_threads = 4
max_depth = 1
